# Integration Testing

Integration testing is used to verify that different system components work correctly when integrated with each other.

## Requirements

Before running integration tests, make sure that you have your application configured correctly, with the database accessible and the main modules operational.

## Running Integration Tests

To run integration tests, use the following command:

```bash
pytest tests/integration