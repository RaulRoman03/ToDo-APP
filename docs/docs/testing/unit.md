# Unit Testing

Unit tests verify the correct functioning of individual functions and methods within the application. These tests ensure that each unit of code behaves as expected in isolation.

## Requirements

To run unit tests, you need to have `pytest` and the project dependencies installed. Make sure you have `pytest` installed in your development environment:

```bash
pip install pytest