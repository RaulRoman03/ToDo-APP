# ToDo-APP Configuration

After installing the application, perform the following configurations to customize your environment.

---

## Environment Variables
Make sure to configure the `variables.env` file with the following parameters:
```env
FLASK_ENV=development
SECRET_KEY=your-secret-key
DATABASE_URL=postgresql://user:password@localhost/todo_db